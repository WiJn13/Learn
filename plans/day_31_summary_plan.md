## Day 31 学习内容总结计划

### 目标
总结 `Learn/day_31.py` 文件中的学习内容，并以清晰、结构化的方式呈现。

### 待办事项
- [ ] 总结列表推导式的使用和示例。
- [ ] 归纳列表操作的三大类方法（原地修改返回None、返回新结果不修改原列表、原地修改并返回数据），并列举代表性方法。
- [ ] 对比 `.sort()` 与 `sorted()`、`.reverse()` 与 `reversed()` 的区别。
- [ ] 解释不同数据类型拥有专属方法的概念。
- [ ] 总结Python中的除法运算（`/`, `//`, `%`）及其特性。
- [ ] 详细说明 `round()` 函数的用法，包括基本舍入、默认行为、银行家舍入法和负数位数舍入。
- [ ] 总结 `lambda` 函数的定义、使用和特点。
- [ ] 深入解析 `.sort()` 方法的 `key` 参数原理（“瞎子裁判与代理人模型”）。
- [ ] 详细解释 `map()` 函数的语法、工作原理（“工厂流水线”模型）、单/多迭代对象处理、惰性求值和“木桶效应”。
- [ ] 将以上总结内容整理成一份易于理解的文档。
